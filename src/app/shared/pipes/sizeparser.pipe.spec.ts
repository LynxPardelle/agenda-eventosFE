import { SizeparserPipe } from './sizeparser.pipe';

describe('SizeparserPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeparserPipe();
    expect(pipe).toBeTruthy();
  });
});

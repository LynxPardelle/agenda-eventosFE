import { CuttingTextPipe } from './cutting-text.pipe';

describe('CuttingTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CuttingTextPipe();
    expect(pipe).toBeTruthy();
  });
});
